# Optional: GitHub token for non-interactive auth (otherwise you'll be prompted)
# GITHUB_TOKEN="ghp_xxxx"

# Where to clone repos (default: ~/code)
TARGET_DIR="$HOME/code"

# Repos to offer for cloning - add your own URLs
# Leave empty to rely on GitHub API fetch only
REPOS=(
    "https://github.com/example/repo1"
)
